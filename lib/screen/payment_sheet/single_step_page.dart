import 'package:flutter/material.dart';

class SingleStepPage extends StatelessWidget {
  const SingleStepPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
